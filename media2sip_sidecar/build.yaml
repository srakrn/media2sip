# Only used when `image:` is removed from config.yaml so the supervisor builds
# locally. Expect a long build: pjproject is compiled from source.
build_from:
  aarch64: ghcr.io/home-assistant/aarch64-base-debian:bookworm
  amd64: ghcr.io/home-assistant/amd64-base-debian:bookworm
